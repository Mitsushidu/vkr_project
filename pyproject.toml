[project]
name = "vkr-project"
version = "0.1.0"
description = "Django scaffold for legal consultation VKR project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.4",
    "python-dotenv>=1.0.1",
    "psycopg2>=2.9.11",
]
